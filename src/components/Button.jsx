import styles from "../style"

const Button = ({styles}) => {
  return (
    <button type='button' className={`bg-blue-gradient py-4 px-6 mt-10 font-poppins font-normal text-[18px] text-primary rounded-[10px]`}>
        Get started
    </button>
  )
}

export default Button